
_target_: lightning.Trainer

# training duration
max_epochs: 10
min_epochs: 1

# checkpointing
default_root_dir: ${original_work_dir}/outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}

# configure logging
log_every_n_steps: 10
